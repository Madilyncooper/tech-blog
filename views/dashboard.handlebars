{{> nav}}



<a id="addNewBlogBtn" href="/blogAdd">Add New Post</a>
<div>
    <div class="card">
        <ul class="card-body">
            {{!-- {{#each blogContent}}
            {{#if this.user_id === id}}
            <li>
                <h2>{{this.title}}</h2>
                <p>{{this.content}}</p>
            </li>
          
            {{/each}}
              {{/if}} --}}
              {{id}}
              {{#each blogContent}}
              <li>
                <h2>{{this.title}}</h2>
                <h2>{{this.content}}</h2>
              </li>
              {{/each}}
        </ul>
        
    </div>
</div>