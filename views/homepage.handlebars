<h1>This is the homepage</h1>
{{> nav}}
<div>
    {{#if name}}
    <p>Welcome back {{name}}</p>
    {{/if}}
</div>

{{#if blogContent.[0].id}}
<div class="card">
    <ul class="card-body">
        {{#each blogContent}}
        <li>
            <h2>{{this.title}}</h2>
            <p>{{this.content}}</p>
            <p>Posted by {{this.user.name}} on {{format_date this.date}}</p>
        </li>
        {{/each}}
    </ul>
</div>
{{else}}
<h2>There are no blog posts at this time. Check back in for new postings!</h2>
{{/if}}
